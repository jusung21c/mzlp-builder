'use babel';
import builder from './BuilderClass'
export default class LexBuilder extends Builder{

    constructor(builder, builderfullpath, file, lang, options) {
        super(builder, builderfullpath, file, lang, options);
    }


}